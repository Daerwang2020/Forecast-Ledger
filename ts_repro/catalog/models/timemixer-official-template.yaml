name: timemixer-official
priority: P0
kind: official
enabled: false
disabled_reason: Production official API bridge is verified; enable only after full-series preprocessing and split mapping are sealed.
adapter: command
mode: supervised
official_repository: https://github.com/kwuking/TimeMixer
repository_dir: ${TS_REPRO_MODEL_ROOT}/timemixer
official_revision: e24610583b36fdd8c76cc17a8df4e65759a5f460
commands:
  train: ["{python}", "${TS_REPRO_BRIDGE_ROOT}/timemixer_bridge.py", train]
  predict: ["{python}", "${TS_REPRO_BRIDGE_ROOT}/timemixer_bridge.py", predict]
official_hyperparameters: Use the paper/official-repo setting and record any explicit overrides here.
